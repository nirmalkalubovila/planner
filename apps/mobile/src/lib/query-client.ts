import { QueryClient } from '@tanstack/react-query';

// Same defaults as apps/web/src/App.tsx's QueryClient.
export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      refetchOnWindowFocus: false,
      retry: 1,
      staleTime: 5 * 60 * 1000,
      gcTime: 10 * 60 * 1000,
    },
  },
});
