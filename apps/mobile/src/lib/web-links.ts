/** The deployed web app — apps/web/public/sitemap.xml and index.html's
 * canonical link are the source of truth for this domain. Used for links
 * out to web-only surfaces (the marketing/(public) landing page, and the
 * privacy/terms pages opened via expo-web-browser's in-app browser sheet). */
export const WEB_BASE_URL = 'https://www.legacylifebuilder.xyz';

export const PRIVACY_POLICY_URL = `${WEB_BASE_URL}/privacy`;
export const TERMS_OF_SERVICE_URL = `${WEB_BASE_URL}/terms`;
