// Moved to @llb/core (platform-agnostic). Re-exported here so existing
// `@/constants/scheduling` imports keep working unchanged.
export {
  DAYS_OF_WEEK,
  SHORT_DAYS,
  SLOTS_PER_DAY,
  type DayOfWeek,
} from '@llb/core';
