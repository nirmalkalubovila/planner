// Moved to @llb/core (platform-agnostic). Re-exported here so existing
// `@/types/domain` imports keep working unchanged.
export {
  Status,
  type GlobalRecords,
  type Habit,
  type Milestone,
  type AIGeneratedPlanSlot,
  type Goal,
  type CustomTask,
} from '@llb/core';
